1) undefined means that variable has declared but not assign as a value  ,
   null means variable is empty
   
2)map() are for Creates and returns a new array by applying a function to every element of the original array.
forEach() it used for create something

3)=== : It compares both the value and the type.
==: It compares values after converting them to a common type

4) async/await in fetching API data  allows the code to pause and wait for the data in the background without freezing the browser.

5) Global Scope: Variables declared outside any function
   Function Scope: Variables declared inside a function
   Block Scope: Variables declared with let or const inside curly braces {}
